int numOfPerfectSquares(int a, int b) {
        // code here
        int count=0;
        for(int i=a;i<=b;i++)
        {
        for(int j=1;j*j<=i;j++)
        {
            if(j*j==i)
            {
                count=count+1;
            }
        }
        }
        return count;
    }
